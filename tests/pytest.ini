[pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short -ra --strict-markers
markers =
    destructive: 会改变同步状态的测试（默认仍跑，可 -m "not destructive" 跳过）
filterwarnings =
    ignore::DeprecationWarning
